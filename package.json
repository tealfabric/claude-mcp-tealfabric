{
  "name": "mcp-server-tealfabric",
  "version": "0.2.0",
  "description": "MCP server for Tealfabric: webapps, ProcessFlow, documents, connectors, integrations (Claude Code, stdio)",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "mcp-server-tealfabric": "dist/index.js"
  },
  "scripts": {
    "build": "tsc && node scripts/sync-plugin-dist.mjs",
    "start": "node dist/index.js",
    "dev": "npm run build && node dist/index.js",
    "validate:marketplace": "node scripts/validate-template.mjs"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  }
}
